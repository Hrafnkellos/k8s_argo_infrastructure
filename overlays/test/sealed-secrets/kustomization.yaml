apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sealed-secrets
resources:
  - ../../../base/sealed-secrets
  # TODO place sealed secret in this directory!
  #- sealed-bitbucket-repo.yaml 