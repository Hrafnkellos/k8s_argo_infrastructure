apiVersion: apps/v1
kind: Deployment
metadata:
  name: petstore-api
  namespace: petshop
  labels:
    app: petstore-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: petstore-api
  template:
    metadata:
      labels:
        app: petstore-api
    spec:
      containers:
      - name: petstore-api
        image: swaggerapi/petstore:latest
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m" 